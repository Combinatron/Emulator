This is a directory of example programs to run in the Combinatron.

Right now it contains a single program [simple.comb](simple.comb). It doesn't do
anything in particular but it is syntactically valid.
